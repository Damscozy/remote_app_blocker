name: remote_app_blocker_example
description: Example app demonstrating remote_app_blocker package usage
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Local path to the package (for development)
  remote_app_blocker:
    path: ../

  # Firebase dependencies for the example
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0
  firebase_remote_config: ^5.0.0

  # Optional: Use for production version detection
  # package_info_plus: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
