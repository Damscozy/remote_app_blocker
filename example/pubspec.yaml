name: remote_app_blocker_example
description: Example app demonstrating remote_app_blocker package usage
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Local path to the package (for development)
  remote_app_blocker:
    path: ../

  # Optional: Use for production version detection
  # package_info_plus: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
